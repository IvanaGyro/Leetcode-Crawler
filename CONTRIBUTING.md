# Contributing

## Commits

Use the [Conventional Commits](https://www.conventionalcommits.org/) format:

```text
<type>[optional scope]: <description>
```

Keep each commit focused on one logical change. Common types include `feat`, `fix`, `docs`, `refactor`, `test`, and `chore`.
