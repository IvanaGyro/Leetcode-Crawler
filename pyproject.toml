[project]
name = "leetcode-crawler"
version = "0.1.0"
description = "Download accepted LeetCode solutions and optionally commit and push them."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "curl-cffi==0.16.0",
    "patchright==1.61.2",
    "pytest>=8,<9",
    "pytest-mock>=3.14,<4",
]

[tool.pixi.workspace]
channels = ["conda-forge"]
platforms = [
    "linux-64",
    "linux-aarch64",
    "osx-64",
    "osx-arm64",
    "win-64",
    "win-arm64",
]

[tool.pixi.target.linux-64.dependencies]
pygit2 = "==1.20.0"

[tool.pixi.target.linux-aarch64.dependencies]
pygit2 = "==1.20.0"

[tool.pixi.target.osx-64.dependencies]
pygit2 = "==1.20.0"

[tool.pixi.target.osx-arm64.dependencies]
pygit2 = "==1.20.0"

[tool.pixi.target.win-64.dependencies]
pygit2 = "==1.20.0"

[tool.pixi.target.win-arm64.pypi-dependencies]
pygit2 = "==1.20.0"

[tool.pixi.tasks]
crawler = { cmd = "python main.py", depends-on = ["install-browser"] }
install-browser = "patchright install chromium"
test = "python -m pytest"

[tool.pytest.ini_options]
testpaths = ["tests"]
